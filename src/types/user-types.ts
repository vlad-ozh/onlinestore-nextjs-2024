export interface IUser {
  // id: string;
  email: string;
  // name: string;
  password: string,
  // isActivated: boolean;
  // isAuth: boolean;
  // selectedProducts: string[];
  // cart: IUserCart[];
};
